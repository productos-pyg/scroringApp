import { UserAuth } from "../context/AuthContext"
import Wrap from "./Wrap"

const Home = () => {
  //const{user} = UserAuth();

  //console.log(user)
  return (
    <Wrap>
          <div>home</div>
    </Wrap>
  )
}

export default Home